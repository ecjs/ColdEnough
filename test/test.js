var chai = require('chai');
var expect = chai.expect;

var c = require('../index');

describe('Testing API', function() {
  it('Able to post to WUNDERGROUND API', function(done) {
    done();
  });
});
